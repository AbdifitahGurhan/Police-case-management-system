'use strict';require('dotenv').config();const db=require('../src/config/database');async function add(n,d){const[r]=await db.query('SHOW COLUMNS FROM police_officers LIKE ?',[n]);if(!r.length)await db.query(`ALTER TABLE police_officers ADD COLUMN \`${n}\` ${d}`)}async function run(){await add('approval_status',"ENUM('PENDING','APPROVED','REJECTED','RETURNED') NOT NULL DEFAULT 'APPROVED'");await add('approval_notes','TEXT NULL');await add('approved_by','VARCHAR(100) NULL');await add('approved_at','DATETIME NULL');await add('state_administration_id','INT NULL');await add('district_id','INT NULL');await db.query(`CREATE TABLE IF NOT EXISTS officer_approval_history(id INT PRIMARY KEY AUTO_INCREMENT,officer_id INT NOT NULL,previous_status VARCHAR(30),new_status VARCHAR(30) NOT NULL,notes TEXT,performed_by VARCHAR(100) NOT NULL,created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,CONSTRAINT fk_oah_officer FOREIGN KEY(officer_id) REFERENCES police_officers(id),INDEX idx_oah_officer(officer_id))`);console.log('Officer approval migration completed.');await db.pool.end()}run().catch(async e=>{console.error(e);await db.pool.end();process.exit(1)});
