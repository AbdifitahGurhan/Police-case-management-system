'use strict';require('dotenv').config();const db=require('../src/config/database');async function run(){const[r]=await db.query("SHOW COLUMNS FROM police_officers LIKE 'mother_name'");if(!r.length)await db.query('ALTER TABLE police_officers ADD COLUMN mother_name VARCHAR(150) NULL AFTER full_name');console.log('Officer personal fields migration completed.');await db.pool.end()}run().catch(async e=>{console.error(e);await db.pool.end();process.exit(1)});
