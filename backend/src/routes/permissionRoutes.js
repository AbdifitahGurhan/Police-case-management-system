'use strict';const router=require('express').Router();const auth=require('../middleware/authMiddleware');const {requirePermission}=require('../middleware/permissionMiddleware');const c=require('../controllers/permissionController');router.use(auth);router.get('/',requirePermission('permissions.manage'),c.getMatrix);router.post('/roles',requirePermission('roles.manage'),c.createRole);router.put('/roles/:roleId',requirePermission('permissions.manage'),c.updateRolePermissions);router.put('/users/:userId',requirePermission('permissions.manage'),c.updateUserPermissions);module.exports=router;
